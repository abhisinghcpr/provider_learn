import 'package:flutter/material.dart';

class DecreaseProvider extends StatefulWidget {
  const DecreaseProvider({super.key});

  @override
  State<DecreaseProvider> createState() => _DecreaseProviderState();
}

class _DecreaseProviderState extends State<DecreaseProvider> {
  @override
  Widget build(BuildContext context) {
    return const Scaffold(

    );
  }
}
